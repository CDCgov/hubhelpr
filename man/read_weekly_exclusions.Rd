% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weekly_exclusions.R
\name{read_weekly_exclusions}
\alias{read_weekly_exclusions}
\title{Read a hub's weekly location exclusions file.}
\usage{
read_weekly_exclusions(path)
}
\arguments{
\item{path}{character, path to the exclusions file. A
file that does not exist reads as no exclusions,
since a hub that has never needed one has nothing
to record.}
}
\value{
Named list of exclusions keyed by reference
date, each element in a form accepted by
\code{\link[=apply_target_location_exclusions]{apply_target_location_exclusions()}}.
}
\description{
The file is TOML keyed by reference date, where each
value takes either form the \code{generate-viz-data}
action accepts for \code{excluded_locations}: an array of
abbreviations applying to every target, or a table
mapping target names (or \code{all}) to arrays.
}
\details{
\if{html}{\out{<div class="sourceCode toml">}}\preformatted{2025-01-01 = \{ all = ["VI"], "wk inc covid hosp" = ["GU"] \}
2025-02-02 = ["AK", "AR"]
}\if{html}{\out{</div>}}

A date with no entry has no exclusions, so the file
lists only the weeks that need one.
}
