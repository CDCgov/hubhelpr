% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location_exclusions.R
\name{get_reference_date_exclusions_list}
\alias{get_reference_date_exclusions_list}
\title{Get a hub's location exclusions for one reference date.}
\usage{
get_reference_date_exclusions_list(hub_reports_path, disease, reference_date)
}
\arguments{
\item{hub_reports_path}{character, path to the
forecast hub reports directory.}

\item{disease}{character, disease name ("covid" or
"rsv").}

\item{reference_date}{character or Date, the reference
date to look up.}
}
\value{
Named list of abbreviations keyed by target (or
by \code{all}), empty when the date has no entry, ready to
pass as \code{excluded_locations}.
}
\description{
Exclusions are recorded per hub in a TOML file keyed by
reference date, where each value takes either form the
\code{generate-viz-data} action accepts for
\code{excluded_locations}: an array of abbreviations applying
to every target, or a table mapping target names (or
\code{all}) to arrays.
}
\details{
\if{html}{\out{<div class="sourceCode toml">}}\preformatted{2025-01-01 = \{ all = ["VI"], "wk inc covid hosp" = ["GU"] \}
2025-02-02 = ["AK", "AR"]
}\if{html}{\out{</div>}}

A date with no entry has no exclusions, so the file
lists only the weeks that need one.
}
